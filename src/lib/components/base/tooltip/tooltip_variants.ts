import { tv } from 'tailwind-variants';

export const tooltipContentVariants = tv({
	base: 'inline-flex items-center gap-1.5 rounded-md px-3 py-1.5 text-[11px] bg-muted text-foreground border border-border-strong shadow-md w-fit max-w-xs',
});
